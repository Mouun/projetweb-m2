import { DestructiveActionButtonDirective } from './destructive-action-button.directive';

describe('DestructiveActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DestructiveActionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
